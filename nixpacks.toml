[phases.setup]
nixPkgs = ["php82Full"]

[phases.build]
cmds = ["php -r 'echo \"PHP Version: \" . phpversion();'", "composer install --no-dev --optimize-autoloader || true"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
